import { ShowServerErrorModule } from './show-server-error.module';
import { TranslateModule } from '@ngx-translate/core';

describe('ShowServerErrorModule', () => {
  let showServerErrorModule: ShowServerErrorModule;

  beforeEach(() => {
    showServerErrorModule  
  });

  it('should create an instance', () => {
    expect(showServerErrorModule).toBeTruthy();
  });
});
